@{
  Layout = "_Layout";
}

@model PierresTreats.Models.Treat

<section class="card mb-3">
  <div class="card-header">
    <div class="cardListName">Edit Treat: @Html.DisplayFor(model => model.Name)</div>
    <div class="cardListButton">
      @using (Html.BeginForm("Delete", "Treats", new {id = Model.TreatId}))
      {
        <button type="submit" class="btn btn-danger">Delete Treat</button>
      }
    </div>
  </div>
  <div class="card-body">
    <div class="card-text">
      @using (Html.BeginForm())
      {
        @Html.HiddenFor(model => model.TreatId)
        @Html.LabelFor(model => model.Name)
        @Html.EditorFor(model => model.Name, new { htmlAttributes = new { @class = "form-control" } })
        <input type="submit" value="Save" class="btn btn-primary mt-3 mb-3" />
      }
    </div>
  </div>
</section>